/*
 * Copyright (c) 2019. Legioner9@inbox.ru
 */
col('*');
var ini_time = performance.now();
var proseed_time;

col('***');
setTimeout(function () {
    proseed_time = performance.now();
    col('proseed_time = ' + proseed_time)
}, 100)

col('**');

function wateFunResult(fun_reserch, res, max) {
    return
    for (var i = 0; i < max; i++) {
        Math._fib(10);
        if (res) return fun_reserch(res);
    };
}

function funing(res) {
    return res - t1 ;
}

wateFunResult(funing, t2, 100)
var after_time = performance.now();

var all_time = {
    'ini_time': ini_time,
    'proseed_time': proseed_time,
    'after_time': after_time,
};

col('****');

col(all_time);


