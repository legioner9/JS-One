/*
 * Copyright (c) 2019. Legioner9@inbox.ru
 */

let re = /[\w]+/ig;
let re_n = new RegExp('[\\w]+', 'gi');
let match = /(привет\ S+)/.exec('Это привет миру!');
let r_1 = 'Fsd Hhfdb'.replace(/(?<one>\w+) (?<two>\w+)/, `$<one> - $<two>`)
let u = {
    str: `<span class="my_1">
<span class="my_2">`,
    regexp: /<(?<result>(?<teg>[a-z]+)\s*(?<volume>[^>]*))>/,
}

re.compile('vfdaf', 'g');
let $re_n = re_n._$.Ess();

let reg_2 = u.regexp.exec(u.str);
let j_reg_2 = JSON.stringify(reg_2);
// let reg_3 = /([w+])/;
let res_reg_3 = /(\w+)/.exec('asdc vgwrfvt ftwv')
debugger;